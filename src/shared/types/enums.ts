export enum DataSteps {
  Idle = "idle",
  Loading = "loading",
  Succeeded = "succeeded",
  Failed = "failed",
}
export enum Order {
  Asc = "asc",
  Desc = "desc",
}
export enum Sort {
  Popular = "popular",
  Name = "name",
}
